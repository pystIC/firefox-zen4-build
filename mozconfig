ac_add_options --enable-application=browser

# Clang-cl flag syntax: -clang: prefix required on Windows for GCC-style flags
ac_add_options --enable-optimize="-O3 -clang:-march=znver4"

# Thin LTO: works across Rust/Clang regardless of LLVM version mismatch
ac_add_options --enable-lto=thin

# Release mode (enables additional release-only optimizations)
ac_add_options --enable-release
ac_add_options --disable-debug
ac_add_options --disable-debug-symbols

# 6 parallel jobs - safe with system-managed pagefile on 32GB
mk_add_options MOZ_MAKE_FLAGS=-j6

# Rust: target Zen 4, max optimization, thin LTO handles cross-module so codegen-units=4 is fine
export RUSTFLAGS="-C target-cpu=znver4 -C opt-level=3 -C codegen-units=4"

# Disable unused subsystems (smaller binary, fewer indirect calls)
ac_add_options --disable-crashreporter
ac_add_options --disable-updater
ac_add_options --disable-maintenance-service
ac_add_options --disable-webrtc
ac_add_options --disable-tests
ac_add_options --disable-accessibility
ac_add_options --disable-stl-hardening
